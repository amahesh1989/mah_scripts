Provision GCE instance with Oracle database created. This uses a Machine image with HA and Oracle binaries preinstalled.

1) Edit parameters in db.config. Instructions in the config file.

2) Invoke provisioning script with desired instance name as arguement.

./provision_oracle_ha.sh <instance_name>
